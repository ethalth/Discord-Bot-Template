# TODO
- [ ] Create ansi helper
- [ ] Make a log class
- [ ] Make help command that actually reads the whole bot directory
- [ ] Make a script that removes every command so other hosts commands are not there.